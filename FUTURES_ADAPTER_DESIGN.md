# 先物ペーパー・アダプタ 設計メモ（2026-08-08）
**status: 設計のみ。実装はユーザー承認後。実弾は一切なし。**

## 目的
crypto で確立したエンジン（データ収集→エッジ→検証→ペーパー→自動キル/昇格）を、**先物（当面は商品季節性）**に横展開する。市場アダプタの差し替えのみで、既存の規律をそのまま適用する。

## ★2026-08-08 ホールドアウト検証で下方修正（重要）
前半で頑健月を選定→後半(完全未知)で測定: **in-sample +3.80%/勝率66% → OOS +0.88%/勝率54%**。
→ 全期間の「+3.71%/67%」は**選定バイアス(多重比較)で膨張していた**。真のforward期待値は**+0.88%/回(口座換算 年+1.5%程度)＝限界的**。
→ **判定下方修正: 先物季節性は"ゼロではないが薄い"。低DD本命プロダクトではない。** 実装するなら極小額のforwardテストまで。crypto(funding/trend=forward実測あり)が依然本命。以下のバスケット構成は参考値(in-sample)。

## 検証済みシード・エッジ（★2026-08-08 季節性バスケット・in-sample参考値）
6商品15年で頑健月(前半後半同符号&|平均|>2%)を抽出→バスケット化。**375トレード(25回/年)・勝率67%・平均+3.71%/回・前半+3.17→後半+4.28(改善)**。
- **極小サイズ7%notional換算で 年率約+6.5%/口座・最大DD約3%(Sharpe≈2)** ＝低DD志向に合致。損切りなし・月末決済。
- 確定バスケット構成(頑健月):
  - NG(天然ガス): 1売 2売 4買 8買 9買 10買 11買 12売
  - ZC(トウモロコシ): 4買 5売 7売 12買
  - CL(原油): 1買 2買 11売
  - GC(金): 1買 8買
  - ZS(大豆): 2買 4買 7売 8売
  - ZW(小麦): 8売 9買 11売 12買
- ⚠️注意: 選別は同データ(多重比較・穀物相関あり)。**forward検証が必須**=ペーパーアダプタの役目。

## データ層
- ソース: Yahoo chart API `query1.finance.yahoo.com/v8/finance/chart/{TICKER}?range=15y&interval=1d`（UA必須・無料）。
- 銘柄: NG=F / ZC=F / CL=F / GC=F 等。日足のみ（季節性は日足で十分）。
- 収集: 既存サーバに `collectFutures()` を1日1回追加、`data/fut_<sym>.jsonl` に追記。
- 注意: Yahooは非公式・レート制限/欠損あり → リトライ＋欠損スキップ。連続限月=ロール減価込み（季節性検証には好都合）。

## シグナル層
- `futuresSeasonalSignal(sym)`: 現在の月が銘柄の頑健月なら dir を返す。月初(=前月末→当月初の営業日跨ぎ)で発火。
- 頑健月テーブルは検証で確定したものをハードコード（例: NG={12:-1,1:-1,2:-1,4:+1,8:+1,9:+1,10:+1,11:+1}）。

## 執行・リスク層（★ここが低DD化の肝）
- ペーパー建玉を `data/futures_paper.json`（crypto paper.json と同型）に記録。equity 別建て or 合算はユーザー選択。
- **★2026-08-08 検証確定: 低DD化は「損切り」でなく「極小サイズ」で行う。**
  - ATR損切りグリッド(1.0/1.5/2.0/3.0)検証結果=**損切りは逆効果**。合計+659%→+281%(1.5ATR)へ半減、勝率65%→44%へ崩壊、しかも裾は−15%止まりで綺麗に切れない(NGは月中mean-reversionで、切ると回復分を取り逃す＋日中ギャップ)。→**損切り採用せず**。
  - 代わりに**固定・極小サイズ**: 最悪−27.8%/回を口座−2%に抑える=建玉≈口座の7%(=リスク約0.07)。エッジを丸ごと保ったまま口座DDを抑制。
- **決済: 月末（seasonal exit）のみ**。損切りなし（サイズで管理）。
- **★複数商品バスケット化**: NG単体は年+3〜5%程度と控えめ。NG＋ZC＋他を極小サイズで束ね、年間トレード数を増やして滑らかに（低DD先物プロダクトの正しい形）。
- 既存の MAX_DD_HALT / 自動キル(A) をそのまま適用（負ければ自動停止）。

## 未確定・次段の検証タスク（実装前に潰す）
1. **ATR損切り付与でNG季節性のファットテールが切れるか**（エッジを保ったまま最大負けを圧縮できるか、grid検証）。
2. 頑健月の**多重比較耐性**（前半後半分割は通過済だが、可能なら別データ源で追試）。
3. ZC/他商品でも同手順を通す（分散）。
4. 実執行の現実性（限月・流動性・実手数料）。日本からの先物執行手段（IBKR等）は別途要確認。

## 統合方針
- crypto エンジンと**同居**（同じ index.js に futures モジュール追加 or 別プロセス）。判断はユーザー。
- 週次スコアカード(C)・自動キル(A)・自動昇格(B)の枠組みを先物にも流用。
